# Frontend — immigration API host only (no /api path).
# Client appends /api/aor-track/v1
NEXT_PUBLIC_API_URL=https://api.example.com

# --- Optional: local backend/ stub only ---
# MONGODB_URI=mongodb://localhost:27017
# MONGODB_DB_NAME=aor-v2
# GEMINI_API_KEY=
# AI_ESTIMATE_MODEL=gemini-2.5-flash
# AI_ESTIMATE_PROMPT_VERSION=v1
# AI_ESTIMATE_MAX_RETRIES=2
# PORT=4000
# CORS_ORIGIN=http://localhost:3000
